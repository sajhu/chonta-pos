services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: chonta
      POSTGRES_PASSWORD: chonta
      POSTGRES_DB: chonta_pos
    ports:
      - "5432:5432"
    volumes:
      - chonta_pos_db:/var/lib/postgresql/data

volumes:
  chonta_pos_db:
